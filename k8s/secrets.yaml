---
# BFF Secrets Template
# Update with your actual values before deploying
apiVersion: v1
kind: Secret
metadata:
  name: bff-secrets
  namespace: bff-service
type: Opaque
stringData:
  # Generate a strong secret for production (use: openssl rand -hex 32)
  jwt-secret: "CHANGE-THIS-TO-A-STRONG-SECRET-KEY"
  # Add other secrets as needed
  # redis-password: ""
